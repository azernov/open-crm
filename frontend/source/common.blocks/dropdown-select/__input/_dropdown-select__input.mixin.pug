mixin dropdown-select__input
    input.dropdown-select__input(class!=attributes.class type=attributes.type ? attributes.type : "text" placeholder=attributes.placeholder ? attributes.placeholder : "Плейсхолдер" value!=attributes.value)
